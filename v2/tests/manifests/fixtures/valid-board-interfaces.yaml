# Fixture: VALID board manifest with an interfaces: identity map.
# Exercises the board.schema.json `interfaces` object (eth0/eth1/wlan0 Path=
# values). manifest.bats asserts a well-formed interfaces block validates (exit 0).
family: rk3588
board_id: test-board
dtb_name: rk3588-test-board.dtb
description: Board fixture with a valid interfaces identity map.
interfaces:
  eth0: platform-fe190000.pcie-pci-0004:41:00.0
  eth1: platform-fe180000.pcie-pci-0003:31:00.0
  wlan0: platform-fe170000.pcie-pci-0002:21:00.0
