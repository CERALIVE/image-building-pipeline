# SIZE_OK: the shared fixture/helper home for the six manifest contract suites;
# splitting it again would put a helper out of reach of a case that uses it.
tests/manifest-helpers.bash
# SIZE_OK: runtime service contracts share the hostname/mDNS and thermal fixtures.
tests/runtime-services.bats
# SIZE_OK: package-set contracts share the parity-rootfs and BSP provenance fixtures.
tests/package-contract.bats
# SIZE_OK: package transport module keeps native/curl transaction state in one process.
lib/fetch-debs.sh
# SIZE_OK: build orchestrator is a linear transaction with shared cleanup state.
lib/orchestrate.sh
# SIZE_OK: preflash verification is one fail-closed ordered media gate.
tests/preflash-verify.sh
# SIZE_OK: RK3588 contract shares expensive disk fixtures across assertions.
tests/rk3588-ab-contract.bats
# SIZE_OK: the disk-assembly ENTRY keeps the geometry transaction (build_disk) and
# the process-level scratch/EXIT-trap registry its lib/disk/ modules all call into;
# the per-concern bodies already live in lib/disk/{repart,slot,boot,gap,verify}.sh.
lib/assemble-disk.sh
# SIZE_OK: the hostname claim script is ONE generated payload that the real-avahi
# contract extracts verbatim and drives; splitting the heredoc splits the harness.
mkosi/customize/postinst.d/hostname.sh
# SIZE_OK: /data migration + the RAUC update entrypoint are generated payloads that
# must stay one transaction with the fstab and bind-mount units that shadow them.
mkosi/customize/postinst.d/persistence.sh
