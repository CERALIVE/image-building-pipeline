# CONNECT only for the first-party mTLS origin; everything else must use a
# cacheable HTTP request (Debian HTTPS uses the build-only HTTPS/// remap).
PassThroughPattern: ^apt\.ceralive\.tv:443$
AllowUserPorts: 80 443
